snake.jar is run without arguments and can be executed from file explorer.
It is run using this command:
    java -jar snake.jar

The game is played using the arrow keys, and wasd.
It can be paused using space, esc, and p.